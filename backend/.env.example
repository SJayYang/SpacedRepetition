# SpaceRep Backend Environment Variables
# Copy this file to .env and fill in your actual values

# ===========================================
# SUPABASE CREDENTIALS
# Get these from: https://supabase.com/dashboard/project/_/settings
# ===========================================

# Database connection (from Settings → Database → Connection String → URI)
# IMPORTANT: Special characters in password must be URL-encoded (e.g., # becomes %23, @ becomes %40)
# Use pooled connection (port 6543) for queries
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@aws-0-us-west-2.pooler.supabase.com:6543/postgres?pgbouncer=true&statement_cache_size=0

# Direct database connection (for migrations and db push - port 5432, not pooled)
DIRECT_URL=postgresql://postgres:[YOUR-PASSWORD]@aws-0-us-west-2.pooler.supabase.com:5432/postgres

# API credentials (from Settings → API)
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=eyJhbGc...your-anon-key-here
SUPABASE_SERVICE_KEY=eyJhbGc...your-service-role-key-here

# ===========================================
# NOTES
# ===========================================

# 1. Replace [YOUR-PASSWORD] with your actual database password (URL-encoded)
# 2. Replace [YOUR-PROJECT-REF] with your Supabase project reference
# 3. Never commit this file to git (it's in .gitignore)
# 4. For production, use environment variable management from your hosting provider
